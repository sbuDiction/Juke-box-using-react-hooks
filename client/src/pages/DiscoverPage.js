import SearchBar from "../components/SearchBar";

const DiscoverPage = () => {
    return (
        <div className="container-right">
            <SearchBar />
        </div>
    );
};

export default DiscoverPage;